name: CI/CD pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configurar entorno
        run: echo "Preparando build..."
      - name: Ejecutar tests
        run: echo "Aquí ejecutarías tus pruebas"
